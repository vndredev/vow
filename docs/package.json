{
  "name": "@vow/docs",
  "version": "0.0.0",
  "private": true,
  "description": "vow's documentation — built with VitePress, kept in sync from day one.",
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview"
  },
  "dependencies": {
    "@vow/emit-primitive": "workspace:*",
    "@vow/headless": "workspace:*",
    "@vow/theme": "workspace:*",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
