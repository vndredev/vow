{
  "name": "@vow/emit-view",
  "version": "0.0.0",
  "private": true,
  "description": "vow's view emitter — turns an `emit view` vow into a typed Vue list over an entity.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vp test",
    "check": "vp check"
  },
  "dependencies": {
    "@vow/core": "workspace:*"
  },
  "devDependencies": {
    "vite-plus": "catalog:"
  }
}
