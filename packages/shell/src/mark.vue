<!-- The vow mark — `[V]`, real type (a bracketed Vermilion V), never an image. Token-driven (the V is the
     accent, the brackets the ink, the box a surface), so it reads on both the light surface and the terminal
     Nacht. `inline-flex` collapses the whitespace between the glyphs; `aria-hidden` — the wordmark labels. -->
<template>
  <span class="vow-shell__mark" aria-hidden="true">
    <span class="vow-shell__mark-br">[</span>
    <span class="vow-shell__mark-v">V</span>
    <span class="vow-shell__mark-br">]</span>
  </span>
</template>
