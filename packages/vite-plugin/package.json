{
  "name": "@vow/vite-plugin",
  "version": "0.0.0",
  "private": true,
  "description": "vow as a Vite plugin — projects the vow tree into the app live (the closed cap).",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./client/bug-reporter": "./src/client/bug-reporter.ts"
  },
  "scripts": {
    "test": "vp test",
    "check": "vp check"
  },
  "dependencies": {
    "@vow/core": "workspace:*",
    "@vow/db": "workspace:*",
    "@vow/emit-bind": "workspace:*",
    "@vow/emit-entity": "workspace:*",
    "@vow/emit-primitive": "workspace:*",
    "@vow/emit-view": "workspace:*",
    "@vow/icons": "workspace:*",
    "@vow/layout": "workspace:*",
    "@vow/observability": "workspace:*"
  },
  "devDependencies": {
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "peerDependencies": {
    "vite": "*"
  }
}
