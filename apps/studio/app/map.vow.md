---
id: vow_map
fulfills: emit view
nav: { label: Map, icon: git-commit, group: Plan }
---

# Map

## view

```yaml
- h1: Map
- p: The plan grouped by north-star pillar — what each capability has open toward its horizon, the throughline (the structure, not the timeline).
- plan: { as: map }
```
