---
id: vow_trace
fulfills: emit view
nav: { label: Trace, icon: git-commit, group: Operations, order: 1 }
---

# Trace

## view

```yaml
- h1: Trace
- p: The live agent-run feed — every run.started, phase, and pr.merged as it happens. The studio's window into what the autonomous loop + its agents are doing right now.
- events: { as: trace }
```
