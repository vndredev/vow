---
id: vow_addtask
fulfills: emit form
---

# Add a task

A bound, validated form over the `task` entity — its fields, zod-checked, with per-field errors.

## form

```yaml
of: task
submit: Add task
```
